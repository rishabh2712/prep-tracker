[build]
  base = "apps/prep-tracker"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20.20.0"

[functions]
  included_files = ["src/**", "data/frontend-bank/**", "scripts/**"]
